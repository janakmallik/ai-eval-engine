[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"


[project]
name = "ai-eval-engine"
version = "0.1.0"
description = "An evaluation and observability engine for AI/ML systems"
requires-python = ">=3.12"
dependencies = []


[tool.setuptools.packages.find]
where = ["src"]